<div class="list-group">
  <% @releases.each do |release| %>
    <%= link_to release["title"], "#",
        class: "list-group-item list-group-item-action",
        data: { 
          action: "click->musicbrainz#selectAlbum",
          id: release["id"]
        } %>
  <% end %>
</div> 