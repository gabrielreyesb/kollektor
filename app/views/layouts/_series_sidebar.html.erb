<!-- Main Navigation -->
<div class="sidebar-content">
  <ul class="nav flex-column">
    <!-- Search fields removed for cleaner look -->
  </ul>
</div>

<!-- Management Section at Bottom -->
<div class="sidebar-footer">
  <ul class="nav flex-column">
    <li class="nav-item">
      <%= link_to statistics_path,
          class: "nav-link",
          style: "color: white; padding: 0.6rem 0.75rem; display: block; text-decoration: none; #{current_page?(statistics_path) ? 'background: rgba(255, 255, 255, 0.2);' : ''}" do %>
        <i class="bi bi-graph-up me-2"></i>Statistics
      <% end %>
    </li>
    <hr>
    <li class="nav-item">
      <%= link_to genres_path(collection_type: 'Series'),
          class: "nav-link",
          style: "color: white; padding: 0.6rem 0.75rem; display: block; text-decoration: none; #{current_page?(genres_path) ? 'background: rgba(255, 255, 255, 0.2);' : ''}" do %>
        <i class="bi bi-music-note-list me-2"></i>Manage Genres
      <% end %>
    </li>
    <li class="nav-item">
      <%= link_to actors_path,
          class: "nav-link",
          style: "color: white; padding: 0.6rem 0.75rem; display: block; text-decoration: none; #{current_page?(actors_path) ? 'background: rgba(255, 255, 255, 0.2);' : ''}" do %>
        <i class="bi bi-person me-2"></i>Manage Actors
      <% end %>
    </li>
    <li class="nav-item">
      <%= link_to series_index_path,
          class: "nav-link",
          style: "color: white; padding: 0.6rem 0.75rem; display: block; text-decoration: none; #{current_page?(series_index_path) ? 'background: rgba(255, 255, 255, 0.2);' : ''}" do %>
        <i class="bi bi-film me-2"></i>Manage Series & Movies
      <% end %>
    </li>
    <li class="nav-item">
      <%= link_to countries_path(collection_type: 'Series'),
          class: "nav-link",
          style: "color: white; padding: 0.6rem 0.75rem; display: block; text-decoration: none; #{current_page?(countries_path) ? 'background: rgba(255, 255, 255, 0.2);' : ''}" do %>
        <i class="bi bi-globe me-2"></i>Manage Countries
      <% end %>
    </li>
  </ul>
</div> 